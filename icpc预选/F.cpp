#include <bits/stdc++.h>
using namespace std;
#define int long long
typedef pair<int, int> PII;

void solve()
{
	int k,x,y;
	cin>>k>>x>>y;
	int fa=1,fa=1;
	int p=1;
	while(x!=y)
	{
		int pp;
		if(p<x&&p<y)
		{
			pp=(p*k)+p;
		}
		if(pp<0)
		{
			break;
		}
	}
	
	
}

signed main() {
	ios::sync_with_stdio(false);
	cin.tie(0);
	cout.tie(0);
	int tt = 1;
		cin >> tt;
	while(tt--) solve();
	
	return 0;
}
