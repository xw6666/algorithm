#include <bits/stdc++.h>
using namespace std;
const int N=1e5+10;
typedef long long LL;
typedef pair<int, int> PII;
#define x first 
#define y second  
int a[N];
void solve()
{
	int n;
	cin>>n;
	for(int i=1;i<=n;i++) cin>>a[i];
	vector<int>b;
	a[0]=-1,a[n+1]=-1;
	for(int i=1;i<=n;i++)
	{
		if(a[i])
	}
}

int main() {
	ios::sync_with_stdio(false);
	cin.tie(0);
	cout.tie(0);
	int tt = 1;
	//	cin >> tt;
	while(tt--) solve();
	
	return 0;
}
